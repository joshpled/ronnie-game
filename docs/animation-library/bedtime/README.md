# Combined bedtime sequence — checkpoint 3

Josh approved the settling checkpoint with its disclosed hip-drop and head-tuck joins. This preview joins the existing approach/circle artwork to that settling sequence and finishes on the exact approved curled sprite. No new artwork is generated or re-cleaned.

The full sequence lasts 13,520 ms. It keeps the approach, circle, sniffing pauses and settling frame durations. The standalone approach/circle preview’s 800 ms endpoint hold is removed; the settling sequence’s 600 ms starting hold supplies one pause before lowering. The final approved sleeping pose holds for 1800 ms. Refined breathing remains unfinished.

Both earlier studies used different scene sizes and slightly different placements inside the bed. The combined scene preserves the approved curled composition: the dog cell sits 32 px right and 44 px above the bed’s top-left corner. Every approach/circle world point moves down by the same 13 px to meet that position. This is a translation of the path only; dog and bed pixels, sprite scales, poses and frame timing remain unchanged. Consecutive world positions match at every join.

All sources remain separate existing assets. The timeline records their paths and each pose, duration and position. HTML embeds that same timeline for file-based playback, while the GIF samples movement at 40 ms intervals. HTML plays once and stops; the GIF repeats. Section controls allow review of the circle, settling or curled endpoint, and layer controls expose the separate bed.

Existing approach foot sliding/repeated headings and the accepted settling joins remain visible. Joining the stages does not fix or approve those motion-quality limitations. No motion is promoted into the approved sheet during this assembly, and the original atlas and all source images remain byte-identical.

Josh approved the complete sequence with the disclosed caveats. It is packaged in the animation-library PR. No app integration or deployment is included.
